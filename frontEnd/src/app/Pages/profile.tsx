import { View, Text } from 'react-native';

// Placeholder screen for the Profile tab.
// This will be replaced with the real profile screen in a future sprint.
export default function Profile() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center', backgroundColor: '#080e1f' }}>
      <Text style={{ color: 'white' }}>Profile Page</Text>
    </View>
  );
}